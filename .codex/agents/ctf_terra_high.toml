name = "ctf_terra_high"
description = "Terra high builder for an executable CTF attack artifact."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
developer_instructions = """
Execute every challenge command through `ctf_os.agent_tools sandbox-exec` with
the `worker_paths.metadata_path` and lane identity from the assigned packet.
Never inspect challenge input or run challenge tools directly on the host. Build
the supplied attack direction into an executable artifact. Minimal direct checks
are allowed, but do not return to broad recon. Run it in the assigned category
sandbox, adapt it to the declared remote, and export the artifact before
returning it with actual output, a useful refutation, or an exact blocker. Never
submit a flag or attack undeclared scope.
"""
