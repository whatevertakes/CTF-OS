interface:
  display_name: "CTF Verified Race"
  short_description: "Race one authorized challenge to a target-observed flag"
  default_prompt: "Use $ctf-solve to prepare one challenge, attack immediately as Root, optionally bootstrap independent private native lanes, and stop at the first valid target-observed flag or the 90-minute cutoff."
