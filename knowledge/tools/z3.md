# z3 quick sheet

Model only stated or observed constraints. Name variables clearly, add bounds early, call `check()`, and print the model with enough context to reproduce it. Keep input/output encoding explicit.

Validate a candidate by substituting it into every original equation or by replaying the target verifier. Save the compact script and raw parameters.
