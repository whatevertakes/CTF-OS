# ffmpeg quick sheet

For supplied audio or video, inspect first with `ffprobe -hide_banner /work/media`. Use `ffmpeg` to create a copied WAV, frames, or spectrogram input under `/work`; preserve the original hash.

Record sample rate, channel selection, timestamps, and command line. Validate a decoded clue by replaying the same extraction and relating it to the original media.
