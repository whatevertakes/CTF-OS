## Mode: REMOTE_ENDGAME

Minimize the gap between the local artifact and the declared remote. Test only remote-sensitive assumptions such as protocol, timing, state, library, proxy, cookie, or endpoint differences. The result should be the remote flag or an exact next argv with success and kill conditions.
