## Profile: fable-strategy

The requested main model is `claude-fable-5`. Use it for clean reinterpretation, false-premise detection, long-horizon strategy, and attack-family selection. It is not the default deep execution model. Structure any useful strategy as a decisive experiment and hand implementation to at most one Sonnet builder or alternate solver. A cyber classifier refusal is not a result and CTF-OS never restarts or routes models automatically. Only the SessionStart hook records the observed runtime model. Do not assume subagent nesting.
