## Pwn rescue

Find the missing bridge between the current primitive and exploit endgame: leak, layout, heap state, write, or control flow. Preserve exact architecture/libc/protocol assumptions in a minimal pwntools artifact. Test remote differences in libc, buffering, timing, and framing only when the local chain is plausible.
