## Forensic rescue

Use the shortest deterministic extraction path and preserve provenance and hashes. Access large inputs through bounded individual imports, not broad copying. Stop once the target bytes or a remote-ready extractor are reproducible.
